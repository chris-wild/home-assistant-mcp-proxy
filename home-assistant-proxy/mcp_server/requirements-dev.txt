# Test dependencies — intentionally excludes mcp (requires Python 3.10+)
# so the logic-layer unit tests can run on Python 3.9.
fastapi==0.115.0
uvicorn==0.30.1
httpx==0.27.2
pydantic==2.9.0
python-json-logger==2.0.7
pytest==8.3.3
pytest-asyncio==0.24.0
